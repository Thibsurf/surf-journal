# meteofetch est GPL-2.0 — isolé dans ce dossier pour ne pas imposer cette
# licence au reste du repo (JS). Aucune clé API : `meteofetch` télécharge les
# GRIB2 depuis le bucket S3 public de Météo-France sans authentification.
# cfgrib/eccodes(lib) sont installés automatiquement en dépendances et
# embarquent leur binaire (pas d'apt-get requis sur ubuntu-latest, vérifié).
meteofetch==0.5.8
pandas==2.3.3
requests==2.32.5

# fetch_mfwam.py — copernicusmarine (EUPL-1.2) : nécessite un compte gratuit
# Copernicus Marine (identifiants passés en CI via COPERNICUSMARINE_SERVICE_
# USERNAME/_PASSWORD, cf. workflow). xarray décode le NetCDF téléchargé.
# xarray>=2024.10.0 EXIGÉ par copernicusmarine (pyproject.toml du projet,
# vérifié le 01/08/2026) : c'est la version qui a introduit le paramètre
# zarr_format sur open_zarr (remplace zarr_version, déprécié depuis 2024.9.1),
# que copernicusmarine==2.2.1 appelle en interne pour lire du zarr-python 3.x.
# Le pin précédent (2024.2.0) était sous ce minimum → chaque run CI plantait
# avec `TypeError: open_zarr() got unexpected keyword arguments zarr_format`,
# indépendamment des identifiants (bug distinct, trouvé le même jour que le
# correctif d'identifiants — les deux masquaient l'un l'autre : le job ne
# passait jamais assez loin pour révéler celui-ci avant que les identifiants
# soient corrigés).
copernicusmarine==2.2.1
xarray==2024.11.0
# h5netcdf (dépendance transitive de copernicusmarine, décode le NetCDF final)
# déclare h5py comme extra OPTIONNEL (`h5py; extra == "h5py"`, vérifié sur
# PyPI le 01/08/2026), pas comme dépendance dure — un `pip install` normal ne
# le tire donc PAS automatiquement. Sans lui : ImportError au moment d'écrire
# le NetCDF (`h5netcdf/core.py:_parse_backend`), après avoir passé
# authentification + téléchargement + décodage zarr avec succès.
h5py==3.16.0

# fetch_ecmwf.py — ecmwf-opendata (Apache-2.0), aucune clé nécessaire (Open
# Data CC-BY-4.0). cfgrib/eccodes déjà présents via meteofetch (cf. plus
# haut), épinglés ici en plus car importés directement (engine="cfgrib").
ecmwf-opendata==0.3.33
cfgrib==0.9.15.0
